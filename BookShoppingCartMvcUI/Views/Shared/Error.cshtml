@model ErrorViewModel
@{
    ViewData["Title"] = "Помилка";
}

<h1 class="text-danger">Помилка.</h1>
<h2 class="text-danger">Сталася помилка під час обробки вашого запиту.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>ID запиту:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Режим розробки</h3>
<p>
    Перехід в <strong>Режим розробки</strong> дозволить відобразити більш детальну інформацію про помилку, що сталася.
</p>
<p>
    <strong>Режим розробки не повинен бути увімкнений для розгорнутих додатків.</strong>
    Це може призвести до відображення чутливої інформації з виключень кінцевим користувачам.
    Для локальної налагодження увімкніть <strong>Режим розробки</strong>, встановивши змінну середовища <strong>ASPNETCORE_ENVIRONMENT</strong> в значення <strong>Development</strong>
    та перезапустіть додаток.
</p>

